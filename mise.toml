[tools]
go = "1.25"

[tasks.build]
description = "Build release packages for all platforms using GoReleaser"
run = "goreleaser release --snapshot --clean --skip=publish"

[tasks.clean]
description = "Clean dist directory"
run = "rm -rf dist"
